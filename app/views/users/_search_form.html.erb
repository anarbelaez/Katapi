<%= form_with url: goals_path, method: :get, html: { class: 'mb-5', data: { refresh_list_target: 'form' } } do |f| %>
  <%= f.text_field :query,
                    class: "form-control",
                    placeholder: 'Type something',
                    value: params[:query],
                    data: {
                    refresh_list_target: 'searchInput',
                  } %>
  <% end %>
