<div class="row d-flex justify-content-end rounded-4 p-2">
  <%= month_calendar events: @tasks do |date, tasks| %>
    <% tasks.each do |task| %>
    <button class="btn btn-sm btn-success rounded-circle bg-success text-center mx-auto" style="width:35px" type="button"
        id="dateTask" data-bs-toggle="dropdown" aria-expanded="false">
        <%= date.day %>
      </button>
      <div class="dropdown">
        <ul class="m-0 dropdown-menu shadow-lg border-2 border-bottom border-end border-info fs-07 rounded-4 bg-body px-2" aria-labelledby="dateTask">
          <% tasks.each do |task| %>
            <li class="m-0 text-center">
             <%= link_to "• #{title_stylized(task.name)}" , task, class:"text-decoration-none fs-08 text-primary" %>
            </li>
          <% end %>
        </ul>
        <div class="dropdown d-none">
    <% end %>
    <div class="d-flex justify-content-center dash-calendar">
      <%= date.day %>
    </div>
  <% end %>
    </div>
  </div>
</div>
