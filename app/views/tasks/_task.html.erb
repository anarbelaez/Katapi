<div id="<%= dom_id task %>" class="table-row d-flex justify-content-start shadow-sm py-1 border-bottom border-primary">
  <div class="table-cell d-inline-block text-truncate" style="width: 120%;">
    <%= link_to "#{task.name}" , task, class:"text-decoration-none text-white-smoke" %>
  </div>
  <div class="table-cell text-white-smoke" style="width: 60%;">
    <%= task.priority %>
  </div>

  <div class="table-cell text-white-smoke" style="width: 60%;">
    <%= task.difficulty %>
  </div>

  <div class="table-cell text-white-smoke d-flex" style="width: 60%;">

    <% if task.not_started? %>
      <span class="badge rounded-pill bg-danger text-dark"> <%= task.status %></span>
    <% elsif task.in_progress? %>
      <span class="badge rounded-pill bg-info text-dark"> <%= task.status %></span>
    <% else %>
      <span class="badge rounded-pill bg-success text-dark"> <%= task.status %></span>
    <% end %>

  </div>

  <div class="table-cell text-white-smoke" style="width: 60%;">
  <small> <%= task.due_date&.strftime("%d/%m/%y") %> </small>
  </div>

</div>
