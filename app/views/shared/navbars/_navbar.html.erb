<nav class="navbar shadow-sm navbar-expand-lg">
  <div class="container-fluid mx-2 py-0">
    <h1><%= link_to "Katapi", activity_path, class: "navbar-brand text-light fw-bold" %></h1>
    <ul class="navbar-nav ms-auto">
      <div class="d-flex">
        <%# Notifications %>
        <%#= render "shared/navbars/notifications" %>

        <%# User actions %>
        <a class="nav-link" href="#" data-bs-toggle="dropdown">
          <%= image_tag get_avatar_url, class: "avatar avatar-sm dropdown-toggle", id: "navbarDropdown", 'aria-haspopup': true, 'aria-expanded': false %>
        </a>

        <ul class="bg-body dropdown-menu dropdown-menu-dark dropdown-menu-end mx-4 me-3 position-absolute mt-1 border-0 rounded-4 shadow-sm" tab-index="-20">
          <%= link_to user_path(current_user), class: "text-decoration-none link-light" do %>
            <li class="dropdown-item rounded-3">
                <i class="bi bi-person-circle me-3"></i>
                <span>Profile</span>
            </li>
         <% end %>

          <%= link_to "#", class: "text-decoration-none link-light" do %>
            <li class="dropdown-item rounded-3">
              <i class="bi bi-bar-chart-line me-3"></i>
              <span>Activity</span>
            </li>
          <% end %>

          <li><hr class="dropdown-divider"></li>
          <%= link_to destroy_user_session_path, data: {turbo_method: :delete}, class: "text-decoration-none link-light" do %>
            <li class="dropdown-item rounded-3 mb-1">
              <i class="bi bi-power me-3"></i>
              <span>Log Out</span>
            </li>
          <% end %>
        </ul>
      </div>
    </ul>
  </div>
</nav>
