<!DOCTYPE html>
<html>
  <head>
    <title>Conoce Katapi</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "pages", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "pages", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <% if current_page?(home_path) %>
      <%= render "pages/shared/navbar_pages" %>
    <% end %>
    <div class="">
      <%= yield %>
    </div>
    <% if current_page?(home_path) %>
      <%= render "pages/shared/footer_pages" %>
    <% end %>
  </body>
</html>
